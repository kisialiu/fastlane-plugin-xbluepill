describe Fastlane::Actions::XbluepillAction do
  describe '#run' do
    it 'prints a message' do
      # expect(Fastlane::UI).to receive(:message).with("The xbluepill plugin is working!")
      #
      # params = Hash.new
      # params[:workspace] = "test.workspace"
      # params[:scheme] = "test"
      # params[:output_dir] = ""
      # Fastlane::Actions::XbluepillAction.run(params)
    end
  end
end
